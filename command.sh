ps -Am | grep '/usr'| head #This command use to list string '/usr' 

grep -c 'libexec' out.txt #To know the occurrences of 'libexec' out.txt file. libexec is the includes internal binaries that are not intended to be executed directly by user.

date #This command can show the current date & time

cal #This command can show the calendar

whoami #This can use to show the curent user

df -i #df is an abbreviation for disk space df -i is to check the number of inodes(unique identifier in a file system.

which python #This command shows the path to python. 
